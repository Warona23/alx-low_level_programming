#include "main.h"
#include <stdio.h>

void _puts(char *str);
/**
 * main - entry point
 * Return: always 0 (success)
 */

int main(void)
{
	puts("\"Programming is like biulding a multilingual puzzle");
	return (0);
}
