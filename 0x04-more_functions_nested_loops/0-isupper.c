#include "main.h"

/**
 * _isupper - function checks if a letter is in uppercase
 * @x: The number to be checked
 * Return: 1 for uppercase or 0 for anthing else
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
		return (0);
}
